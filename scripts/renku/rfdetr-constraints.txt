# Deliberately independent of the legacy YOLO/Datumaro environment.
rfdetr==1.10.1
torch==2.10.0
torchvision==0.25.0
pycocotools==2.0.11
supervision==0.30.2
roboflow==1.4.2

# APIs inspected against the selected RF-DETR release's published metadata.
transformers==5.1.0
pytorch-lightning==2.6.1
torchmetrics==1.8.2
torch-hungarian==0.1.0rc0
numpy>=2.2,<2.4
pandas>=2.3,<3
opencv-python-headless>=4.10,<5

# Fail resolution if a future transitive requirement introduces a GUI cv2 wheel.
opencv-python<0
opencv-contrib-python<0
opencv-contrib-python-headless<0

# Build helpers used by the editable project and YOLOX installs.
setuptools>=69
wheel

# Helpers missing from Renku's existing Jupyter environment.
jupytext
ipywidgets

# Scientific and data runtime.
numpy<2.2
pandas
matplotlib
scipy
scikit-learn
pillow
pyyaml
tqdm
psutil
requests
polars
pycocotools
python-dotenv

# Object-detection runtimes and YOLOX support packages.
ultralytics
ultralytics-thop
loguru
thop
ninja
tabulate
tensorboard

# Roboflow requires the headless distribution, while the training packages
# require the regular distribution. Keep both at the same cv2 version.
opencv-python==4.10.0.84
opencv-python-headless==4.10.0.84
supervision
roboflow
datumaro
